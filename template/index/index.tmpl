{{define "title"}}Pictures{{end}}
{{define "head"}}{{end}}
{{define "content"}}
<div class="container">
    <div class="page-header">
        <h1>Index</h1>
    </div>
    <button type="submit" class="btn btn-default" disabled="{{.index_running}}" formaction="index/start">Index</button>
    {{template "footer" .}}
</div>
{{end}}
{{define "foot"}}{{end}}